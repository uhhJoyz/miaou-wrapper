_: {
  config.vim.autocomplete = {
    blink-cmp = {
      enable = true;
      mappings = {
        confirm = "<tab>";
        next = "<c-j>";
        previous = "<c-k>";
        scrollDocsUp = "<c-u>";
        scrollDocsDown = "<c-d>";
      };
    };
  };
}
